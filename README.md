# Нужно подумать что вообще делать с feature слоем! Есть вариант чтоб отказаться там от слайсов, получается в feature будут переиспользуемые фичи ценные для бизнеса а слой widget в свою очередь будет построен вокруг слайсов то есть то есть мы там берем фичи которые не привязаны и переиспользуемые и конкретезруем его под сушности(то-есть слайсы в widget)

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.
